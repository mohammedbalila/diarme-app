import 'package:flutter/material.dart';

class PenIconText extends StatelessWidget {
  const PenIconText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '🖊️',
      style: TextStyle(fontSize: 50.0, fontWeight: FontWeight.bold),
    );
  }
}
